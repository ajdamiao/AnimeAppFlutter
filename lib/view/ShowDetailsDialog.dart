import 'package:flutter/material.dart';

class ShowDetailsDialog extends StatelessWidget {
  const ShowDetailsDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ,
    );
  }
}
